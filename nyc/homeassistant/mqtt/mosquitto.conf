# Mosquitto is only reachable from the home lab network, so anonymous
# connections are allowed rather than managing broker credentials.
listener 1883
allow_anonymous true

persistence true
persistence_location /mosquitto/data/
